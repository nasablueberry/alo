# Environment variables for the client (Vite)
# Copy this file to .env.local and fill in the values

# Backend API URL — set this to your deployed backend URL (e.g. Render)
# Leave empty for local development (Vite proxy handles it)
VITE_API_URL=
